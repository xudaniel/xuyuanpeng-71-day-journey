{
  "name": "journey-dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "python3 scripts/build.py && vite build",
    "build:static": "python3 scripts/build.py",
    "test": "python3 scripts/build.py && node --test tests/*.test.mjs",
    "db:generate": "drizzle-kit generate"
  },
  "devDependencies": {
    "@openai/sites-vite-plugin": "0.2.0",
    "drizzle-kit": "0.31.9",
    "drizzle-orm": "0.45.1",
    "qrcode": "1.5.4",
    "vite": "8.0.13"
  }
}
