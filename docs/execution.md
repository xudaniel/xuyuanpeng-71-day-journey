# 七十一日执行中心

解锁 `private.html` 后，今日页面首先显示执行中心。公开入口保留行程概览和登录入口，不读取个人执行记录。

## 每日使用

1. **今日队列**：按 P0（今天必须做）、P1（今天应该做）、P2（可选）排列。每级内先逾期，再按截止时间排列。点击统计卡可查看对应事项；同一行动即使同时是跟进和 Waiting For，也只出现一次。
2. **待办与跟进**：新增行动、选择优先级、安排日期、负责人及目标日期；可关联会面或交通。等待反馈时填写“等待谁、等待什么、下次跟进”。完成时填写交付／确认记录，取消时填写原因。
3. **会面与活动**：准备清单可标记完成或不适用（需原因）。准备度按适用项计算。查看缺项、打开已有准备卡，然后显式标记准备就绪。
4. **成果**：记录实际发生时间，再分别填写成果摘要、对方陈述、现场观察、个人判断和出处。创建后续行动或明确记录无需跟进，才能进入跟进阶段。
5. **关闭**：所有关联行动（含等待反馈）已完成或取消后，手动关闭会面。新增或重新打开关联行动会重新打开会面。阶段更正需要原因，并保留记录。
6. **交通住宿**：默认显示未来七天未确认项目；当日及确认截止已到期的安排进入每日提醒。确认或取消安排会同步处理其专属确认行动。普通关联待办的完成不会自动确认预订。

阶段为 **Planned → Prepared → Completed → Notes → Follow-up → Closed**。已经发生却没有登记准备的会面可补录实际完成，但必须说明原因，不会虚构准备记录。阶段核验与执行中心分别记录；日期经过不代表实际完成。

## 日期与初始资料

- 自然日使用 Asia/Shanghai；仅日期的截止事项在该日结束后逾期。具体时刻须包含 UTC 偏移，如 `2026-09-22T14:00+08:00`。界面上的计划会面时间使用所选当地时区。
- “下一场会面”显示有计划日期且尚未完成的下一场会面；日期未知的会面在“日期待定”中复核，不会猜测日期。
- 首次使用连接现有五家企业的准备／成果文档。公开文档中日期不确定的会面保持日期待定，保留已知阶段范围。已有问题草稿不自动算完成，也不自动创建承诺。
- 根据解锁后的行程阶段生成交通及住宿**待核对记录**，初始状态为未核验。日期是阶段起点，标题明确标注待核对；这不代表该日有预订。用户须核对日期、拆分实际交通／住宿或填写取消原因。
- 后续人工编辑的执行记录是本机唯一可编辑来源；原 Markdown 模板是参考资料，不自动回写，也不与设备记录双向同步。
- 进入、退出 71 天范围时，执行中心显示尚未开始／已结束，逾期事项仍可处理。旧行程卡保留其原有范围内日期浏览行为。

## 就绪预警与所有活动的执行流程

- **就绪信息**衡量资料完整度；**准备清单**衡量本人是否已复核。前者达到 READY 不会自动勾选后者或推进阶段。会面与参访按 11 项计算（3 个关键问题分别计数）；交通／住宿按 9 项计算。每项不适用都须填写原因并从分母剔除，全不适用时显示无适用项目，不显示 READY。
- 会面的日期、时间、地点须明确确认；联系人从安排记录读取。交通票务、住宿的确认状态及出发时刻从同一安排记录读取；票务／住宿只有在相应类型的安排已确认后才算完整，不能仅凭填了一段描述代替确认。其他信息在“更新就绪信息”中维护。
- 关键项为会面日期、时间、地点、联系人，以及交通票务、出发时间、出发站点、住宿、证件要求。未来 24 小时内缺关键项显示 **P0 预警**；未来 72 小时内有缺项显示 **P1 预警**。原行动的用户优先级不改写；今日队列的有效显示优先级取行动与预警中较高者。计数与对应筛选使用同一队列。
- 定时活动按实际时区比较。仅有日期时，从该日北京时间起点保守提醒，未知时间仍明确显示为未知，不写入假定时刻。日期完全未知的活动留在日期待定视图。已经登记完成的活动不再出现出发前的就绪预警。
- 准备、就绪、待核验和交通确认提醒关联同一条记录时，在今日队列合并为一行并保留各个标记。真正不同的关联行动仍各自显示。今日首先显示下一条就绪预警，可直接打开原记录补齐资料。
- 交通／住宿记录自动关联一条执行流程。普通待办可点“启用执行流程”将其作为重要任务管理；会面与参访可直接新增。修改原任务或交通安排会同步流程中的计划信息，不建立另一份可冲突编辑的安排。
- 六阶段规则同样适用于会面、参访、行程、任务。预订确认与实际完成分别记录。重要任务登记实际完成后同步完成其原行动；关闭活动仍需保存成果、复核跟进并解决关联行动。交通安排未核验／待确认时不能关闭其流程。
- 新增或重新打开关联行动会使 Closed 活动回到 Follow-up，包括重新打开交通确认。原成果及历史保留。更正完成记录后，原时间仍保留为历史登记，不再表示当前阶段已经完成。
- 计划日期／时刻经过但尚未登记实际发生时，显示 **Needs Verification · 待核验**，不会直接变为 Completed。可按六个阶段筛选，使用“Closed · 已关闭档案”搜索标题、联系人、城市与成果。

## 联系人与后续行动

行动可记录人物／公司、城市、行程阶段、沟通渠道和备注。等待回复可记录发出请求日期、最近联系日期，以及 1–365 天的无回复提醒间隔；超时从最近联系日期（没有时使用请求日期）计算，不伪造截止日期或改写优先级。原目标期限和下次跟进时间独立显示与计算。

新增后续行动时可选今天、明天、三天后、一周后的快捷目标日期（相对于当前查看日期），或填写自定义日期。实际完成后会提示安排下一步；保存成果后正式复核后续行动或说明无需跟进，不跳过成果记录步骤。进入“人物历史”输入人物或公司，可查看包括已完成行动与已关闭活动在内的往来记录。

旧版本加密数据保持兼容。解锁／恢复时在内存中为旧交通记录补充 Planned 流程，下一次成功保存时一并加密保存；已有确认或过去日期不会变为实际完成。版本仍为 1，已有密码与备份可继续使用。

## 保存与备份

记录仅保存在当前浏览器／设备的 `journey-execution-encrypted-v1` 中。PBKDF2-SHA-256（310,000 次，随机盐）派生独立 AES-256-GCM 密钥；每次保存使用新的随机 IV。密码和密钥不写入存储。保存成功才提交界面状态；失败保留输入并明确提示。另一窗口修改记录后，旧窗口需要重新解锁，不能覆盖更新。

使用“导出加密备份”定期保存副本。恢复时上传该文件并输入**备份保存时的密码**，确认替换当前记录。清除浏览器数据、换设备或更换访问密码前，应先导出备份。更换密码后若旧设备记录无法解锁，请保留加密记录，在原密码仍可使用的入口导出；在新设备／浏览器解锁新入口后，用原密码恢复备份，再保存为新密码加密的记录。没有旧密码无法恢复旧记录。

安全模式移除执行中心的私密 DOM；重新解锁前不会读取新的私密视图。打印和既有日历导出不包含执行记录。锁定会重新载入密码页；浏览器返回缓存也会重新锁定。

## 维护与验证

网站仍为静态文件，无运行时依赖、账户、服务器数据库或发送通知服务。

- `app/model.mjs`：日期、提醒、优先级、准备度、会面状态规则及关联行动。
- `app/vault.mjs`：设备加密记录、备份及并发写入检查。
- `app/ui.mjs` / `app/execution.css`：移动界面及事件处理。
- `app/boot.mjs`：解锁后的初始化。
- `private.html`：只修改登录外壳，解密成功后加载执行中心。原行程密文、盐、IV 和密码机制保持不变；不需要把完整行程提交为明文。

开发使用 Node 24 和 pnpm 11：

```sh
pnpm install --frozen-lockfile
pnpm test
pnpm exec playwright install chromium
pnpm test:browser
```

浏览器检查可通过 `BROWSER_EXECUTABLE` 指定已有 Chromium 可执行文件。可选 `PLAYWRIGHT_MODULE` 指向预装 Playwright 的入口。生产部署不需要安装这些开发工具。

自动检查包括日期边界与时区、去重与排序、Waiting For、准备度、24/72 小时预警边界、旧数据迁移、会面／任务／行程完整生命周期、关闭与重开、无跟进路径、交通状态、加密与错误密码、备份恢复、存储失败和跨窗口冲突。浏览器测试用**临时生成的合成密文**运行生产登录代码，不使用或解密真实私人行程；验证移动界面、加密后刷新、完整工作流、安全模式和原日历导出。真实密码及真实密文的人工登录检查仍须由持有密码的人完成。

对应问题（含后续扩展 #13、#14）：[#5](https://github.com/xudaniel/xuyuanpeng-71-day-journey/issues/5)、[#6](https://github.com/xudaniel/xuyuanpeng-71-day-journey/issues/6)、[#7](https://github.com/xudaniel/xuyuanpeng-71-day-journey/issues/7)、[#8](https://github.com/xudaniel/xuyuanpeng-71-day-journey/issues/8)、[#9](https://github.com/xudaniel/xuyuanpeng-71-day-journey/issues/9)、[#13](https://github.com/xudaniel/xuyuanpeng-71-day-journey/issues/13)、[#14](https://github.com/xudaniel/xuyuanpeng-71-day-journey/issues/14)。
